# Tests

This directory is intentionally empty until an approved implementation task defines the behavior and selected technology defines the test framework.

Keep tests organized to mirror the application boundaries where practical. Every implemented task must identify its `TEST-NNN` evidence and cover the acceptance criteria at the least expensive reliable test level. Document setup and commands here after technology selection.

Tests must be deterministic, isolated from production data and secrets, and explicit about any unavailable external validation.
