# アプリケーション

このファイルは、アプリを初めて実装したタスクで、実際のアプリに合わせて更新します。以後も、機能、構成、アーキテクチャ、設定、実行方法が変わるたびに更新します。

実装前は、承認済みの要件、設計、技術選定、実装タスクが揃うまで、このフォルダーへアプリのコードを追加しません。実装後は、下の説明用の文を削除し、確認できた事実だけを記載します。未実装の計画は「未実装」または「今後の予定」と明記します。

## アプリの内容

- アプリ名:
- 目的:
- 主な利用者:
- できること:
- 今回できないこと:

## 構成

主要なフォルダーとモジュールの役割を記載します。生成物、依存パッケージの保存先、認証情報、ローカルの秘密情報、本番データは含めません。

```text
src/
  README.md
```

## アーキテクチャ

- 全体構成:
- 主要コンポーネントと責務:
- データの流れ:
- 外部サービスとの接続:
- 関連する設計判断: `ADR-NNN`

承認済みの設計と、実際に実装された構成の差がある場合は、その差と理由を記載します。

## 動作環境

| 項目 | バージョンまたは条件 |
| --- | --- |
| ランタイム | |
| パッケージ管理 | |
| 対応OSまたはブラウザー | |

## セットアップ

必要な依存関係の導入手順を、リポジトリのルートから実行できる形で記載します。

```powershell
# 実際のセットアップコマンドへ置き換える
```

## 設定

必要な環境変数や設定ファイルを記載します。値の例には秘密情報や実際の認証情報を使いません。

| 名前 | 必須 | 用途 | 安全な例 |
| --- | --- | --- | --- |
| | | | |

## 実行方法

```powershell
# 実際の起動コマンドへ置き換える
```

起動後に開くURL、ポート、または操作の入口を記載します。

## ビルドと品質確認

| 用途 | コマンド | 確認できること |
| --- | --- | --- |
| ビルド | | |
| 静的解析またはLint | | |
| 自動テスト | | |

実際に成功を確認したコマンドだけを記載します。パスはソースツリー、コマンドはパッケージスクリプトやビルド設定と照合し、記載するコマンドを実行して確認します。ビルドやLintなどが存在しない場合は、空欄にせず `該当なし` と理由を記載します。実行できなかった確認は、理由と残るリスクを明記します。

## 主な操作

アプリの主要な操作を、利用者が再現できる最小限の手順で記載します。

1. `[主要な操作手順へ置き換える]`

## トラブルシューティング

既知の起動失敗、設定不足、ポート競合などと、その確認方法を記載します。

## 関連文書

- 要件: `REQ-NNN`
- 実装タスク: `TASK-NNN`
- テスト: `TEST-NNN`
- 設計判断: `ADR-NNN`
